# Archive Service - Учебный проект на Go

Проект представляет собой REST API для работы с системой архивов документов. Проект разработан в рамках изучения Go и демонстрирует различные аспекты разработки на этом языке.

## Бизнес-логика

Система позволяет запрашивать документы из региональных архивов. Пользователь может:
- Просматривать список документов с пагинацией
- Получать информацию о конкретном документе
- Запрашивать загрузку документа из регионального архива

При запросе документа создается асинхронная задача, которая обрабатывается через очередь сообщений. Система параллельно обращается к внешним сервисам архивов в различных регионах для поиска и загрузки документов.

## Технологический стек

- **Go 1.24+** - основной язык разработки
- **Gin** - HTTP веб-фреймворк
- **PostgreSQL** - реляционная база данных
- **pgx/v5** - драйвер для работы с PostgreSQL
- **Swagger** - документация API
- **Docker Compose** - оркестрация сервисов

## Архитектура

Проект следует принципам чистой архитектуры с разделением на слои:

```
cmd/hello-go/          # Точка входа приложения
internal/
  ├── controller/      # HTTP обработчики (слой представления)
  ├── service/         # Бизнес-логика
  ├── repository/      # Работа с БД
  ├── domain/          # Доменные модели и ошибки
  ├── dto/             # Data Transfer Objects
  └── mapper/          # Преобразование между слоями
migrations/            # SQL миграции
docs/                  # Swagger документация
```

## Реализованный функционал

### ✅ REST API
- `GET /archive/documents` - получение списка документов с пагинацией
- `GET /archive/documents/:id` - получение документа по ID
- `POST /archive/documents/:id` - создание запроса на загрузку документа

### ✅ Swagger документация
- Автоматическая генерация документации из аннотаций
- Доступна по адресу: `http://localhost:8080/swagger/index.html`

### ✅ Работа с БД
- Подключение к PostgreSQL через connection pool
- Миграции базы данных (golang-migrate)
- Транзакции для обеспечения целостности данных
- Работа с JSONB полями
- Использование ENUM типов

### ✅ Обработка ошибок
- Кастомные типы ошибок домена
- Централизованная обработка ошибок в контроллерах
- Валидация входных данных

## Планируемый функционал

### 🔄 Работа с очередями
**Цель**: Асинхронная обработка запросов на загрузку документов

**Реализация**:
- Интеграция с RabbitMQ или Redis Streams
- Публикация задач в очередь при создании запроса
- Worker'ы для обработки задач из очереди
- Retry механизм для неудачных запросов

**Бизнес-логика**: При создании запроса на загрузку документа, задача помещается в очередь. Отдельные воркеры обрабатывают эти задачи, обращаясь к внешним сервисам архивов.

### 🔄 Горутины и конкурентность
**Цель**: Параллельная обработка запросов к нескольким региональным архивам

**Реализация**:
- Использование `sync.WaitGroup` для ожидания завершения горутин
- Каналы для передачи результатов между горутинами
- Context для отмены операций
- Worker pool для ограничения количества одновременных запросов

**Бизнес-логика**: При обработке запроса система параллельно обращается к архивам всех регионов (или указанного региона) для поиска документа. Результаты собираются и сохраняются в БД.

### 🔄 Вызовы внешних сервисов
**Цель**: Интеграция с внешними API региональных архивов

**Реализация**:
- HTTP клиент с таймаутами и retry логикой
- Обработка различных HTTP статусов
- Парсинг ответов от внешних сервисов
- Circuit breaker для защиты от недоступных сервисов

**Бизнес-логика**: Для каждого региона существует внешний сервис архива. Система делает HTTP запросы к этим сервисам для поиска и загрузки документов.

### 🔄 Дополнительные возможности

**Graceful shutdown**:
- Корректное завершение работы при получении сигналов
- Завершение обработки текущих запросов
- Закрытие соединений с БД и очередями

**Логирование**:
- Структурированное логирование (logrus/zap)
- Разные уровни логирования
- Логирование запросов и ответов

**Метрики и мониторинг**:
- Prometheus метрики
- Health check endpoints
- Метрики производительности

**Тестирование**:
- Unit тесты для сервисов
- Integration тесты для репозиториев
- HTTP тесты для контроллеров
- Моки для внешних сервисов

**Конфигурация**:
- Использование viper или envconfig
- Конфигурация через переменные окружения
- Валидация конфигурации при старте

**Валидация**:
- Расширение валидации входных данных
- Кастомные валидаторы для бизнес-правил

## Установка и запуск

### Требования
- Go 1.24+
- Docker и Docker Compose
- Make (опционально)

### Запуск через Docker Compose

```bash
# Запуск БД и миграций
docker-compose up -d

# Запуск приложения
go run cmd/hello-go/main.go
```

Приложение будет доступно по адресу: `http://localhost:8080`

### Переменные окружения

```bash
DATABASE_URL=postgres://postgres:postgres@localhost:5432/archive_db?sslmode=disable
PORT=8080
```

### Генерация Swagger документации

```bash
swag init -g cmd/hello-go/main.go
```

## Примеры использования

### Получение списка документов
```bash
curl "http://localhost:8080/archive/documents?page=1&size=10"
```

### Получение документа по ID
```bash
curl "http://localhost:8080/archive/documents/{uuid}"
```

### Создание запроса на загрузку документа
```bash
curl -X POST "http://localhost:8080/archive/documents/{uuid}?region=MOSCOW" \
  -H "sender: user@example.com"
```

## Структура БД

### Таблица `archive.requests`
Хранит запросы на загрузку документов:
- `id` - UUID запроса
- `document_id` - UUID документа
- `region` - регион архива
- `sender` - отправитель запроса
- `created_at` - время создания
- `completed_at` - время завершения

### Таблица `archive.documents`
Хранит документы:
- `id` - UUID документа
- `data` - JSONB данные документа
- `created_at` - время создания
- `updated_at` - время обновления

## Развитие проекта

Проект можно расширить следующими возможностями:

1. **Аутентификация и авторизация** - JWT токены, роли пользователей
2. **Кэширование** - Redis для кэширования часто запрашиваемых документов
3. **Rate limiting** - ограничение количества запросов от одного пользователя
4. **WebSocket** - уведомления о статусе обработки запросов
5. **Файловое хранилище** - загрузка и хранение файлов документов
6. **Экспорт данных** - экспорт документов в различные форматы
7. **Поиск** - полнотекстовый поиск по документам
8. **Версионирование API** - поддержка нескольких версий API

## Лицензия

Учебный проект
